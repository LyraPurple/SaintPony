{% extends 'base.html.twig' %}

{% block body %}

    <table>
        <thead>
            <tr>
                <th>Nom</th>
                <th>Description</th>
            </tr>
        </thead>

        <tbody>
            {% for pupuduits in prowduits %}
                <tr>
                    <td>{{ pupuduits.nom_produit }}</td>
                    <td>{{ pupuduits.description }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

{% endblock %}
